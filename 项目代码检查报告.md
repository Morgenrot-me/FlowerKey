# FlowerKey 项目代码检查报告

生成日期：2026-04-25

## 检查范围

本次检查覆盖：

- `packages/core`：加密、数据层、同步引擎、WebDAV backend。
- `packages/ui`：扩展共享 UI、store、设置页。
- `packages/extension`：Manifest V3、background service worker、content script。
- `packages/mobile`：Vue 页面、Pinia store、SQLite 数据层、Capacitor/Android 原生代码。
- `packages/desktop`：Vue 页面、Pinia store、Tauri 配置。
- `scripts`：版本同步脚本。
- 构建验证：`pnpm build:core`、`pnpm build:extension`、`pnpm build`。

## 验证结果

| 命令 | 结果 | 说明 |
|---|---|---|
| `pnpm build:core` | 通过 | core TypeScript 构建通过。 |
| `pnpm build:extension` | 通过 | extension Vite 构建通过。 |
| `pnpm build` | 已修复后通过 | 初查时 mobile 构建失败，修复 `packages/mobile/src/pages/SettingsTab.vue:105:9` invalid end tag 后已通过。 |
| `powershell -Command "Set-Location 'packages/mobile/android'; .\\gradlew.bat --no-daemon --max-workers=1 \"-Dorg.gradle.jvmargs=-Xms64m -Xmx1024m -XX:CICompilerCount=2 -XX:ReservedCodeCacheSize=64m -XX:+HeapDumpOnOutOfMemoryError\" \"-Dorg.gradle.parallel=false\" :app:assembleRelease"` | 通过 | 生成 `app-arm64-v8a-release.apk` 和 `app-armeabi-v7a-release.apk`。 |

初始失败摘要：

```text
packages/mobile build:
[vite:vue] src/pages/SettingsTab.vue (105:9): Invalid end tag.
ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL @flowerkey/mobile@0.4.2 build: `vite build`
```

Android 环境说明：默认 `assembleRelease` 在当前 JDK 24 / Windows pagefile 环境下多次出现 JVM native OOM；代码编译错误修复后，使用单工作线程、低堆内存并只构建 `:app:assembleRelease` 验证通过。

## P0：阻塞构建 / 明确错误

### P0-1 移动端 SettingsTab 模板多余结束标签导致全量构建失败

- 位置：`packages/mobile/src/pages/SettingsTab.vue`
- 现象：同步按钮后存在多余 `</button>`，Vue 编译报 invalid end tag。
- 影响：`pnpm build` 和移动端构建失败，阻塞发布。
- 修复建议：删除多余结束标签，校正模板结构后运行移动端构建验证。

### P0-2 移动端设置页错误使用 core Dexie db

- 位置：`packages/mobile/src/pages/SettingsTab.vue`
- 现象：移动端真实数据层是 SQLite，但设置页导入 `@flowerkey/core` 的 Dexie `db` 并调用：
  - `db.getConfig('bookmarkEncrypt')`
  - `db.setBookmarkEncryption(...)`
  - `db.setConfig('bookmarkEncrypt', ...)`
  - `db.importBookmarks(...)`
- 影响：移动端书签加密开关、书签导入、配置读写可能落到错误数据库，导致移动端功能异常或数据不同步。
- 修复建议：改用 `packages/mobile/src/db-sqlite.ts` 数据层，并在 SQLite 层补齐等价能力。

## P1：数据一致性 / 安全关键问题

### P1-1 deviceId 生命周期不完整，changelog 可能写空或 unknown

- 位置：
  - `packages/core/src/db.ts`
  - `packages/mobile/src/db-sqlite.ts`
  - `packages/ui/src/stores/main.ts`
  - `packages/desktop/src/stores/main.ts`
  - `packages/mobile/src/stores/main.ts`
  - `packages/ui/src/stores/sync.ts`
  - `packages/desktop/src/stores/sync.ts`
  - `packages/mobile/src/stores/sync.ts`
- 现象：store 只保存 `deviceId` 配置，没有稳定调用数据层 `setDeviceId()`；同步 store 在缺失时 fallback `'unknown'`。
- 影响：changelog 中 `deviceId` 为空或 unknown，远端 oplog 设备归属错误，可能导致跳过/重复拉取和冲突判断异常。
- 修复建议：
  1. setup/unlock/checkSetup/loadConfig 时确保生成真实 deviceId。
  2. 立即注入数据层 `setDeviceId(deviceId)`。
  3. sync 缺失 deviceId 时主动生成或报错，不使用 unknown。

### P1-2 导入数据、导入书签、书签加密切换绕过 changelog

- 位置：
  - `packages/core/src/db.ts`
  - `packages/ui/src/stores/main.ts`
  - `packages/desktop/src/stores/main.ts`
  - `packages/mobile/src/stores/main.ts`
  - `packages/ui/src/components/SettingsPage.vue`
  - `packages/desktop/src/pages/SettingsTab.vue`
  - `packages/mobile/src/pages/SettingsTab.vue`
- 现象：部分批量写入直接 `put`/`bulkPut`，不写 changelog。
- 影响：本地数据变化不会推送到 WebDAV/iCloud，同步后其他设备缺失变更。
- 修复建议：封装数据层方法，由数据层统一写 entries 与 changelog；批量操作也要为每条变更记录 update/create。

### P1-3 恢复码迁移逻辑错误

- 位置：
  - `packages/ui/src/stores/main.ts`
  - `packages/desktop/src/stores/main.ts`
  - `packages/mobile/src/stores/main.ts`
- 现象：修改主密码时，代码把旧主密码当作 recoveryCode 调用 `decryptMasterPwdWithRecovery(...)`。
- 影响：已有恢复码无法正确迁移；用户可能误以为恢复码仍有效。
- 修复建议：修改主密码后要求重新生成恢复码，或要求用户输入恢复码后迁移；不能静默失败。

### P1-4 移动端 secret config 加密格式与 core 不一致

- 位置：
  - `packages/core/src/db.ts`
  - `packages/mobile/src/db-sqlite.ts`
- 现象：core 使用 `[version + iv + ciphertext]`，移动端使用 `[iv + ciphertext]`。
- 影响：多端无法互读加密配置；WebDAV 密码等 secret config 迁移/同步存在兼容问题。
- 修复建议：统一为带版本字节格式，并在移动端读取时兼容旧格式。

### P1-5 SyncEngine 锁不是原子锁，释放锁可能删除别的设备锁

- 位置：`packages/core/src/sync/engine.ts`
- 现象：`acquireLock()` 使用 read-then-write；`releaseLock()` 直接删除 `sync.lock`，不校验 owner/token。
- 影响：多设备并发同步时可能同时获得锁，或 A 设备释放 B 设备新锁。
- 修复建议：锁内容增加 token；释放前读取并校验 deviceId/token；WebDAV backend 后续可进一步支持 ETag/条件写。

### P1-6 SyncEngine checkpoint 使用本地时间可能漏拉远端 oplog

- 位置：`packages/core/src/sync/engine.ts`
- 现象：`lastSyncTime` 使用本地 `Date.now()`，远端文件名 timestamp 来自其他设备时钟。
- 影响：设备时钟偏差时可能永久漏拉部分远端操作。
- 修复建议：checkpoint 基于“已处理远端文件名/最大远端 timestamp”，不要用本地当前时间作为远端水位。

### P1-7 vault.enc 压缩后普通 sync 不自动 restore

- 位置：`packages/core/src/sync/engine.ts`
- 现象：`maybeCompact()` 会生成 `vault.enc` 并清理旧 oplog，但普通 `sync()` 只拉 oplog。
- 影响：新设备或长时间离线设备可能因为 oplog 被清理而缺失历史数据。
- 修复建议：sync 时检测本地是否需要 restore 快照；或在 oplog 水位缺口时先加载 `vault.enc`。

### P1-8 create/update payload 缺失仍可能标记日志已同步

- 位置：`packages/core/src/sync/engine.ts`
- 现象：push 阶段 create/update 找不到 entry 时可能继续标记 changelog synced。
- 影响：真实变更丢失且不会再推送。
- 修复建议：payload 缺失时保留 unsynced，或者将其转为 delete/skip 并记录明确错误。

### P1-9 Android Application 保存密钥数组未防御性拷贝

- 位置：`packages/mobile/android/app/src/main/java/com/flowerkey/app/FlowerKeyApp.java`
- 现象：`setUnlocked(byte[] dbKey, byte[] masterKey, ...)` 保存传入数组引用。
- 影响：调用方清零局部数组时可能把全局解锁态密钥一起清零，Autofill 后续失效。
- 修复建议：`Arrays.copyOf(...)` 保存副本；调用方局部 `masterKey` 用后清零。

### P1-10 Android Manifest 安全策略不适合密码管理应用

- 位置：`packages/mobile/android/app/src/main/AndroidManifest.xml`
- 现象：
  - `android:allowBackup="true"`
  - `android:usesCleartextTraffic="true"`
- 影响：应用数据可能进入系统备份；WebDAV 明文 HTTP 可能泄露凭据和同步流量。
- 修复建议：改为 `allowBackup="false"`、`usesCleartextTraffic="false"`。如确需 HTTP，应做显式高级选项和风险提示。

## P2：功能可靠性 / 配置问题

### P2-1 Tauri CSP 可能阻止桌面端 WebDAV

- 位置：`packages/desktop/src-tauri/tauri.conf.json`
- 现象：`connect-src` 只允许 `ipc:` 和 `http://ipc.localhost`。
- 影响：桌面生产环境访问用户 WebDAV HTTPS 地址可能被 CSP 阻止。
- 修复建议：允许 `https:`，或通过 Tauri native command 代理网络请求；避免无脑 `*`。

### P2-2 桌面 Cargo 版本与根版本不一致

- 位置：`packages/desktop/src-tauri/Cargo.toml`
- 现象：Cargo 版本为 `0.3.0`，根版本为 `0.4.2`。
- 影响：桌面构建/发布元信息不一致。
- 修复建议：运行 `pnpm version:sync` 或等价同步版本。

### P2-3 MV3 service worker 回收后解锁态不一致

- 位置：`packages/extension/background/service-worker.ts`
- 现象：主密码只存在 service worker 内存；worker 被回收后，`chrome.storage.session` 仍可能显示已解锁。
- 影响：UI 以为已解锁，但 background 无法恢复 dbKey 或填充密码。
- 修复建议：明确 worker 回收后的重新解锁流程；不要只依赖 session 标记表达可填充状态。

### P2-4 Content script 面板拖拽变量未生效

- 位置：`packages/extension/content/index.ts`
- 现象：拖拽更新 `panelX/panelY`，但 `updatePositions()` 不读取这两个变量。
- 影响：面板拖拽交互无效。
- 修复建议：让 `updatePositions()` 使用面板坐标，并做边界夹取。

### P2-5 closed shadow DOM 外部点击判断可能误关面板

- 位置：`packages/extension/content/index.ts`
- 现象：用 `host.contains(e.target)` 判断外部点击。
- 影响：closed shadow DOM 场景下判断不够可靠。
- 修复建议：使用 `e.composedPath().includes(host)`。

### P2-6 public manifest 版本陈旧

- 位置：`packages/extension/public/manifest.json`
- 现象：版本为 `0.1.7`，正式 manifest 为 `0.4.2`。
- 影响：人工打包/误用时版本和权限不一致。
- 修复建议：确认是否仍需要该文件；如保留，应由版本同步脚本维护。删除文件需先确认。

### P2-7 Autofill enabled 判断不准确

- 位置：`packages/mobile/android/app/src/main/java/com/flowerkey/app/AutofillStatePlugin.java`
- 现象：`hasEnabledAutofillServices()` 只表示系统有 Autofill 服务启用，不代表花钥是当前服务。
- 影响：设置页可能误显示已启用。
- 修复建议：读取系统当前 Autofill service 配置并比对花钥服务组件名。

### P2-8 Android Autofill SQLite 文件名需确认

- 位置：
  - `packages/mobile/src/db-sqlite.ts`
  - `packages/mobile/android/app/src/main/java/com/flowerkey/app/FlowerKeyAutofillService.java`
- 现象：TS DB 名是 `flowerkey`，Java 读取 `flowerkeySQLite.db`。
- 影响：如果 Capacitor SQLite 实际文件名不一致，AutofillService 查不到条目。
- 修复建议：确认插件实际文件名；必要时集中定义常量或在 Java 侧兼容实际路径。

## P3：文档和维护性问题

### P3-1 项目说明未列 desktop 包

- 位置：`CLAUDE.md`
- 现象：Monorepo 结构列出 core/ui/extension/mobile，但未列出 desktop。
- 影响：新会话或新维护者容易漏掉桌面端。
- 修复建议：补充 `packages/desktop` 说明。

### P3-2 mobile/desktop 缺少目录中文说明文档

- 位置：`packages/mobile`、`packages/desktop`
- 现象：项目规范要求每个目录有中文 md 说明，当前未发现 mobile/desktop 对应说明。
- 影响：维护规范不完整。
- 修复建议：补齐目录说明文档，内容包括目录作用、文件列表、技术说明、使用示例、扩展方式、注意事项。

### P3-3 若干加密/编码实现可加固

- 位置：
  - `packages/core/src/crypto.ts`
  - `packages/core/src/db.ts`
  - `packages/mobile/src/db-sqlite.ts`
- 现象：
  - verify hash 比较不是 constant-time。
  - `String.fromCharCode(...bytes)` 对大数据有调用栈风险。
  - `generatePassword()` 只限制最小长度，没有上限。
- 影响：安全边界和健壮性不足。
- 修复建议：实现 constant-time 比较；封装 Uint8Array/base64 分块转换；限制密码长度合理上限。

## 建议修复顺序

1. 修复移动端模板 invalid end tag，恢复构建。
2. 修复移动端设置页错误数据层调用。
3. 补齐 SQLite 书签导入、书签加密切换，并写 changelog。
4. 补齐三端 deviceId 生命周期，去掉 unknown fallback。
5. 修复导入/书签加密切换绕过 changelog。
6. 修复恢复码迁移逻辑。
7. 统一 secret config 加密格式并兼容旧格式。
8. 加固 SyncEngine 锁、checkpoint、compact/restore、payload 缺失处理。
9. 修复 Android 密钥数组拷贝、Manifest 安全配置、Autofill 判断。
10. 修复 Tauri CSP、版本同步和文档缺失。
11. 运行构建验证。

## 建议验证命令

```bash
# 构建核心库
pnpm build:core

# 构建浏览器扩展
pnpm build:extension

# 构建移动端前端
pnpm --filter @flowerkey/mobile build

# 构建全部包
pnpm build
```

如修改 Android 原生代码，还应验证 APK 构建。默认完整 `assembleRelease` 在低内存 Windows 环境可能触发 Gradle/JVM native OOM；本次通过以下更保守命令完成验证：

```powershell
powershell -Command "Set-Location 'packages/mobile/android'; .\gradlew.bat --no-daemon --max-workers=1 \"-Dorg.gradle.jvmargs=-Xms64m -Xmx1024m -XX:CICompilerCount=2 -XX:ReservedCodeCacheSize=64m -XX:+HeapDumpOnOutOfMemoryError\" \"-Dorg.gradle.parallel=false\" :app:assembleRelease"
```

APK 输出：

```text
packages/mobile/android/app/build/outputs/apk/release/app-arm64-v8a-release.apk
packages/mobile/android/app/build/outputs/apk/release/app-armeabi-v7a-release.apk
```
